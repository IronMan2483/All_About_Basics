# __Plotly__

interactive plot to click in the values/data and enlarge if needed, need an extra command to see it in Jupyter Notebooks, in VSC visible

#### lineplot:

px.line(data, x=data['column1'], y= data['column2'])
